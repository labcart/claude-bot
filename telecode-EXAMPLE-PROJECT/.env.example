# ============================================================================
# Telegram Bot Configuration
# ============================================================================

# Your bot token from @BotFather on Telegram
# Get it by talking to https://t.me/BotFather and creating a new bot
TELEGRAM_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz1234567890

# ============================================================================
# Authorization - Permanent Allowlist
# ============================================================================

# Comma-separated list of Telegram chat IDs that should always have access
# To get your chat ID: run the bot, message it, and check the logs for "Unauthorized chat: 123456789"
ALLOWED_CHAT_IDS=123456789,987654321

# Auto-trust the first person who messages the bot (1 = yes, 0 = no)
# Set to 1 for easy first-time setup, then set to 0 for security
ALLOW_FIRST_CHAT=0

# ============================================================================
# Authorization - Token-Based Authentication (Optional)
# ============================================================================

# Enable token-based authentication for self-service access
# false = only allowlist works, true = users can authenticate with token
SECURITY_SECRET_REQUIRED=false

# Shared secret token that users send to authenticate
# Leave empty if SECURITY_SECRET_REQUIRED=false
# Generate a secure token: openssl rand -hex 16
SECURITY_SECRET_TOKEN=

# Session timeout for token-authenticated users
# 0 = never expire (permanent after auth)
# Examples: 7d (7 days), 24h (24 hours), 30m (30 minutes), 60 (60 seconds)
SESSION_TIMEOUT=0

# ============================================================================
# Claude Configuration
# ============================================================================

# Claude CLI command (default: claude)
# Change this if claude is installed with a different name or path
CLAUDE_CMD=claude

# ============================================================================
# Advanced Configuration (Optional)
# ============================================================================

# Project directory - defaults to current working directory if not set
# PROJECT_DIR=/path/to/your/project
